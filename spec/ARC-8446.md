# ARC-8446: Encrypted Bus Protocol

| Field     | Value                                                        |
|-----------|--------------------------------------------------------------|
| Number    | ARC-8446                                                     |
| Title     | Encrypted Bus Protocol                                       |
| Version   | 1.2                                                          |
| Lineage   | RFC 8446 (TLS 1.3) key schedule and verify-before-decrypt    |
| Status    | IMPLEMENTED                                                  |
| Author    | HERMES Contributors, Clan JEI (Security Review)              |
| Created   | 2026-03-07                                                   |

---

## 1. Abstract

ARC-8446 specifies the cryptographic protocol for authenticated and encrypted
communication between HERMES clans over the inter-clan relay. It defines key
generation, key exchange, message encryption, digital signatures, and the
verify-before-decrypt pattern that enables secure inter-agent messaging without
shared pre-existing secrets.

The protocol uses three algorithms as a unified stack:

- **Ed25519** for digital signatures (authenticity + integrity)
- **X25519** for Diffie-Hellman key agreement (shared secret derivation)
- **AES-256-GCM** for authenticated encryption (confidentiality)

This combination provides forward-compatible security while remaining
lightweight enough for file-based relay transport.

## 2. Terminology

The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD",
"SHOULD NOT", "RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be
interpreted as described in [ARC-2119](ARC-2119.md).

### 2.1 Definitions

**Clan Key Pair** -- A pair of asymmetric key pairs owned by a clan: one
Ed25519 key pair for signing and one X25519 key pair for Diffie-Hellman key
agreement. Together they form the clan's cryptographic identity.

**Shared Secret** -- A 256-bit symmetric key derived from X25519 key agreement
between two clans. Used as the AES-256-GCM encryption key.

**Sealed Message** -- A message that has been encrypted with AES-256-GCM and
signed with Ed25519 over the ciphertext.

**Fingerprint** -- A human-readable representation of a public key, used for
in-person verification. Format: 8 groups of 4 hex characters separated by
colons (e.g., `a1b2:c3d4:e5f6:7890:1234:5678:9abc:def0`).

**Relay** -- A shared storage location (e.g., private Git repository) where
clans exchange sealed messages via per-clan outbox files.

**Associated Authenticated Data (AAD)** -- Additional data bound to the
ciphertext via AES-256-GCM that is authenticated but not encrypted. Prevents
metadata tampering without revealing sensitive content.

## 3. Cryptographic Stack

### 3.1 Algorithm Selection

| Layer           | Algorithm    | Purpose                          | Key Size |
|-----------------|-------------|----------------------------------|----------|
| Signature       | Ed25519     | Authenticity + integrity         | 256 bits |
| Key Agreement   | X25519      | Shared secret derivation         | 256 bits |
| Encryption      | AES-256-GCM | Authenticated encryption         | 256 bits |
| Key Derivation  | HKDF-SHA256 (RFC 5869) | Uniform key from DH raw output | 256 bits |

### 3.2 Rationale

- **Ed25519 over ECDSA**: Deterministic signatures, faster verification, no
  nonce reuse vulnerability, constant-time implementation.
- **X25519 over ECDH-P256**: Simpler API, resistant to implementation errors,
  designed for key agreement specifically.
- **AES-256-GCM over AES-256-CBC**: Provides both confidentiality and integrity
  in a single operation (AEAD). No need for separate HMAC.
- **HKDF-SHA256 over raw SHA-256**: HKDF (RFC 5869) provides domain separation via the `info` parameter, preventing cross-protocol key confusion. While X25519 output is already uniform, HKDF's extract-then-expand structure aligns with TLS 1.3 §7.1 best practices and future-proofs the key schedule for extended key material derivation.

### 3.3 Why Not PSK (Pre-Shared Key)?

A symmetric-only approach (e.g., AES-256 with pre-shared keys) was considered
and rejected for the following reasons:

| Criterion           | PSK (AES-only)           | Ed25519 + X25519 + AES-256-GCM |
|---------------------|--------------------------|--------------------------------|
| Key distribution    | O(n²) — unique key/pair  | O(n) — one key pair per clan   |
| Key rotation        | Manual coordination      | New DH per session (possible)  |
| Non-repudiation     | No (shared key)          | Yes (unique signature)         |
| Implementation      | ~50 lines                | ~80 lines                      |
| Migration cost      | Requires rekey to scale  | None — scales natively         |

The marginal implementation cost (~30 lines) is far outweighed by the
architectural benefits of asymmetric cryptography.

## 4. Key Generation

### 4.1 Clan Key Pair

A conforming implementation MUST generate two independent key pairs:

1. **Signing key pair**: Ed25519 (32-byte private key, 32-byte public key)
2. **Key agreement key pair**: X25519 (32-byte private key, 32-byte public key)

The two key pairs MUST be generated independently. An implementation MUST NOT
derive one from the other.

### 4.2 Key Storage

Private keys MUST be stored locally with restricted file permissions
(e.g., `0600` on UNIX systems). Private keys MUST NOT be transmitted over any
channel.

Public keys SHOULD be stored in the relay's `keys/` directory as JSON files
with the following format:

```json
{
  "clan": "<clan_id>",
  "ed25519_pub": "<hex-encoded 32-byte Ed25519 public key>",
  "x25519_pub": "<hex-encoded 32-byte X25519 public key>",
  "fingerprint_sign": "<fingerprint of Ed25519 public key>",
  "fingerprint_dh": "<fingerprint of X25519 public key>",
  "generated": "<ISO 8601 timestamp>"
}
```

### 4.3 Fingerprint Computation

Each public key has its own fingerprint. The fingerprint MUST be computed as:

```
fingerprint = SHA-256(raw_public_key_bytes)
            → take first 16 bytes (128 bits)
            → encode as hex
            → format as 8 groups of 4 hex chars separated by ':'
```

Example: `2a37:fb25:a054:93ee:a608:41fc:4ad9:687e`

Fingerprints enable in-person verification of public key authenticity,
following the Trust On First Use (TOFU) model established by Signal's
safety numbers.

## 5. Key Agreement

### 5.1 Shared Secret Derivation

Two clans derive a shared secret using X25519 Diffie-Hellman:

```
raw_shared = X25519(my_dh_private, peer_dh_public)
shared_secret = HKDF-SHA256(ikm=raw_shared, salt=None, info=b"HERMES-ARC8446-v1", length=32)
```

Implementations MUST use HKDF-SHA256 with `info=b"HERMES-ARC8446-v1"` to derive
a uniformly distributed 256-bit key suitable for AES-256-GCM. The `info`
parameter provides domain separation, ensuring that the derived key is bound
to this specific protocol version.

### 5.2 Symmetry Property

Both parties derive the same shared secret:

```
HKDF-SHA256(X25519(A_private, B_public)) == HKDF-SHA256(X25519(B_private, A_public))
```

This property is fundamental to Diffie-Hellman and MUST be verified in any
conformance test suite.

## 6. Message Sealing (Encrypt + Sign)

### 6.1 Sealing Procedure

To seal a message for transmission:

1. **Derive shared secret** per Section 5.1
2. **Generate nonce**: 12 random bytes (96-bit IV for AES-256-GCM)
3. **Construct AAD**: Build the Associated Authenticated Data per Section 6.1.1
4. **Encrypt**: `ciphertext = AES-256-GCM.encrypt(shared_secret, nonce, plaintext_utf8, aad=aad_bytes)`
5. **Sign**: `signature = Ed25519.sign(sender_sign_private, ciphertext_bytes)`
6. **Assemble** the sealed message envelope

#### 6.1.1 AAD Construction

The AAD binds envelope metadata to the ciphertext cryptographically, preventing
header manipulation attacks. The AAD MUST be constructed as a canonical JSON
object containing the envelope fields, encoded as UTF-8 bytes:

```
aad_bytes = canonical_json({"dst": dst, "src": src, "ts": ts, "type": type})
```

Canonical JSON is defined as: keys sorted alphabetically, no whitespace
(`separators=(',', ':')` in Python's `json.dumps`). This ensures both parties
produce identical byte sequences regardless of insertion order.

The AAD object MUST include the following fields from the ARC-5322 envelope:
- `src` — sender clan identifier
- `dst` — destination clan identifier
- `type` — message type
- `ts` — timestamp

The sealed message envelope SHOULD include the AAD as a hex-encoded `aad` field
for diagnostic purposes. The receiver MUST reconstruct the AAD from the outer
ARC-5322 envelope fields and provide it during decryption. If the AAD does not
match, AES-256-GCM decryption will fail with an authentication error, indicating
that the envelope metadata was tampered with.

For backward compatibility with pre-AAD messages: if the sealed envelope does
not contain an `aad` field, the receiver SHOULD decrypt without AAD (`aad=None`).
This allows graceful migration from unbound to bound messages.

### 6.2 Sealed Message Format

```json
{
  "ciphertext": "<hex-encoded ciphertext + GCM tag>",
  "nonce": "<hex-encoded 12-byte nonce>",
  "signature": "<hex-encoded 64-byte Ed25519 signature>",
  "sender_sign_pub": "<hex-encoded 32-byte Ed25519 public key>"
}
```

All binary values MUST be hex-encoded for JSON compatibility.

**Security note on `sender_sign_pub`**: The `sender_sign_pub` field is included
for message routing and debugging purposes only. Receivers MUST verify
signatures using the pre-established public key from `keys/<clan_id>.pub`, NOT
the `sender_sign_pub` value from the sealed envelope. Using the envelope's
`sender_sign_pub` for verification enables identity substitution attacks where
an attacker replaces both the signature and public key to impersonate a
legitimate sender.

### 6.3 Signature Scope

The signature MUST be computed over the **ciphertext bytes** (the raw bytes of
the encrypted output, not the hex string). This is the verify-before-decrypt
pattern from TLS 1.3:

```
signature = Ed25519.sign(private_key, bytes.fromhex(ciphertext_hex))
```

**Rationale**: Signing the ciphertext allows the recipient to verify message
authenticity *before* decryption. This prevents processing of unauthenticated
data and reduces the attack surface.

An implementation MUST NOT sign the plaintext, the nonce, or any combination
other than the ciphertext bytes alone.

## 7. Message Opening (Verify + Decrypt)

### 7.1 Opening Procedure

To open a sealed message:

1. **Verify signature**: `Ed25519.verify(peer_sign_public, ciphertext_bytes, signature)`
2. If verification fails → REJECT message, return `None`
3. **Derive shared secret** per Section 5.1
4. **Reconstruct AAD**: Build the AAD from the outer envelope fields per Section 6.1.1
5. **Decrypt**: `plaintext = AES-256-GCM.decrypt(shared_secret, nonce, ciphertext, aad=aad_bytes)`
6. **Decode**: UTF-8 decode the plaintext bytes

The `peer_sign_public` used in step 1 MUST be the key loaded from
`keys/<clan_id>.pub`, NOT the `sender_sign_pub` from the sealed envelope
(see Section 6.2 security note).

### 7.2 Verify-Before-Decrypt

An implementation MUST verify the Ed25519 signature before attempting
decryption. This ordering is REQUIRED because:

- It prevents wasting computational resources on unauthenticated data
- It provides defense-in-depth against oracle attacks
- It aligns with TLS 1.3 record processing (RFC 8446, Section 5.2)

If signature verification fails, the implementation MUST NOT attempt
decryption and SHOULD log the failure for auditing.

## 8. Relay Transport

### 8.1 Relay Structure

The relay is a shared storage location with the following structure:

```
relay/
├── keys/
│   ├── <clan_a>.pub       # Clan A public keys
│   └── <clan_b>.pub       # Clan B public keys
├── relay/
│   ├── <clan_a>_outbox.jsonl   # Messages FROM Clan A
│   └── <clan_b>_outbox.jsonl   # Messages FROM Clan B
└── relay/payloads/
    └── .gitkeep                # Large payloads (encoding: "ref")
```

### 8.2 Relay Message Format

Each line in an outbox file is a JSON object conforming to ARC-5322 with the
sealed message in the `msg` field:

```json
{
  "ts": "2026-03-07",
  "src": "<sender_clan>",
  "dst": "<receiver_clan>",
  "type": "hello_ack",
  "msg": "<sealed message JSON as string>",
  "ttl": 7,
  "ack": []
}
```

The `msg` field contains the sealed message envelope (Section 6.2) serialized
as a JSON string.

### 8.3 First Contact (Bootstrap)

The first inter-clan exchange follows this sequence:

1. Both clans generate key pairs (Section 4.1)
2. Both clans publish public keys to `keys/<clan_id>.pub`
3. Fingerprints are verified in person (Section 4.3)
4. Clan A seals a `hello` message and writes to `relay/<clan_a>_outbox.jsonl`
5. Clan B reads, verifies, decrypts → seals a `hello_ack` response
6. Clan A reads and verifies the `hello_ack`
7. Bidirectional encrypted channel is established

## 9. Security Considerations

### 9.1 Threat Model

ARC-8446 assumes the following adversary model:

**Assets under protection:**
- Message confidentiality (plaintext content)
- Message authenticity (sender identity)
- Message integrity (no tampering)
- Envelope integrity (metadata binding via AAD)

**Adversary capabilities (in scope):**
- **Attacker-in-the-relay**: Full read/write access to the shared relay storage.
  Can read, modify, delete, or inject messages in any outbox file. This is the
  primary threat model — the relay is untrusted.
- **Network observer**: Can observe all data in transit to/from the relay
  (e.g., Git clone/push traffic). Mitigated by transport encryption (HTTPS/SSH)
  plus ARC-8446 end-to-end encryption.

**Adversary capabilities (out of scope):**
- **Endpoint compromise**: An attacker with access to a clan's private keys or
  local filesystem is outside the scope of ARC-8446. Endpoint security is the
  responsibility of each clan's operational security practices.
- **Side-channel attacks**: Timing attacks, power analysis, or other
  implementation-level side channels are not addressed by this specification.
- **Denial of service**: An attacker deleting relay contents or preventing
  access is not mitigated. Availability is outside scope.

**Security guarantees provided:**
| Threat                        | Mitigation                                    | Section |
|-------------------------------|-----------------------------------------------|---------|
| Message forgery               | Ed25519 signatures                            | §6.3    |
| Message eavesdropping         | AES-256-GCM encryption                        | §6.1    |
| Metadata tampering            | AAD binding                                   | §6.1.1  |
| Key impersonation             | TOFU + fingerprint verification               | §4.3    |
| Identity substitution         | Verify against pre-established keys, not envelope | §6.2 |
| Replay attacks                | Nonce registry + TTL                          | §9.6    |
| Key compromise (current)      | Key revocation protocol                       | §9.7    |
| Key compromise (historical)   | ECDHE forward secrecy (§11.2)                | §9.5    |

### 9.2 Nonce Reuse

AES-256-GCM is catastrophically broken if a nonce is reused with the same key.
Implementations MUST generate nonces using a cryptographically secure random
number generator (`os.urandom` or equivalent). With 96-bit random nonces, the
collision probability remains negligible for up to 2^32 messages per key pair.

### 9.3 Private Key Protection

Private keys MUST be stored with filesystem-level access control. On UNIX
systems, private key files MUST have permissions `0600` (owner read/write
only). Private keys MUST NOT be committed to version control, logged, or
transmitted.

### 9.4 Trust Model

ARC-8446 uses Trust On First Use (TOFU) with in-person fingerprint
verification. This is suitable for the current small-clan model. Future
extensions (see Section 11) MAY introduce certificate authorities or web-of-
trust models for larger deployments.

### 9.5 Forward Secrecy

The base protocol (Sections 5–7) uses static DH keys for key agreement, which
means compromise of a clan's X25519 private key allows decryption of all past
messages encrypted under that static shared secret.

Section 11.2 specifies **per-message ephemeral Diffie-Hellman key exchange
(ECDHE)** that provides forward secrecy. Implementations SHOULD adopt ECDHE
mode (Section 11.2) to protect historical messages against future key
compromise. Once both parties support ECDHE, they SHOULD enable
`require_ecdhe=true` to enforce forward secrecy as a mandatory property.

### 9.6 Replay Protection

Implementations MUST track received nonces to detect and reject replayed
messages. The nonce-set MUST be maintained per sender clan, since nonces are
only required to be unique within a single sender's message stream.

The nonce-set SHOULD be bounded by the TTL window: nonces from messages whose
TTL has expired MAY be evicted from the set. This bounds memory usage while
maintaining replay protection for all messages that are still considered valid.

The nonce-set MUST be persisted across sessions (e.g., to a local file) to
prevent replay attacks that exploit application restarts. On startup, an
implementation MUST load the persisted nonce-set before processing any
incoming messages.

If nonce-set persistence is not feasible (e.g., embedded environments), the
implementation MUST document this limitation and SHOULD employ an alternative
mechanism such as monotonic timestamps validated against a local clock.

### 9.7 Key Revocation

A clan whose private key is compromised MUST initiate key revocation
immediately. The revocation procedure is as follows:

1. **Generate new key pair**: The compromised clan generates a new Ed25519 +
   X25519 key pair per Section 4.1.
2. **Sign a REVOKE message**: Using the OLD (compromised) signing key, the clan
   signs a `REVOKE` message containing the new public keys. Signing with the
   old key proves that the revocation originates from the legitimate key holder
   rather than an attacker.
3. **Publish to relay**: The `REVOKE` message is written to the clan's outbox
   on the relay.
4. **Notify peers out-of-band**: The clan MUST notify all peer clans through an
   out-of-band channel (e.g., in-person, secure messaging) that key revocation
   has occurred.

The `REVOKE` message format:

```json
{
  "ts": "<ISO 8601 timestamp>",
  "src": "<clan_id>",
  "dst": "*",
  "type": "revoke",
  "msg": "<sealed envelope containing new public keys JSON>",
  "ttl": 30,
  "ack": []
}
```

The sealed payload MUST contain the new public keys in the same format as
`keys/<clan_id>.pub` (Section 4.2).

Peer clans MUST stop accepting messages signed with the revoked key
immediately upon receiving and validating the `REVOKE` message. Peer clans
MUST NOT trust the new public keys from the `REVOKE` message alone -- re-keying
REQUIRES the same in-person fingerprint verification as first contact
(Section 8.3).

Revocation is effective immediately upon relay propagation. Messages signed
with the revoked key that arrive after the `REVOKE` message has been processed
MUST be rejected.

## 10. Reference Implementation

The reference implementation is in `reference/python/hermes/crypto.py`:

- `ClanKeyPair` — Key generation, storage, fingerprinting
- `derive_shared_secret()` — X25519 + HKDF-SHA256
- `encrypt_message(key, plaintext, aad=None)` / `decrypt_message(key, nonce, ciphertext, aad=None)` — AES-256-GCM with optional AAD
- `sign_message()` / `verify_signature()` — Ed25519
- `seal_bus_message(plaintext, sender, receiver, aad=None)` / `open_bus_message(sealed, sender, receiver, aad=None)` — High-level seal/open with optional AAD

The `aad` parameter accepts bytes and is passed directly to AES-256-GCM as
Associated Authenticated Data. Callers SHOULD construct AAD per Section 6.1.1.
When `aad` is `None`, no associated data is bound (backward-compatible with
pre-AAD sealed messages).

Test suite: `reference/python/tests/test_crypto.py` (44 tests)

Dependencies: `cryptography>=41.0` (via pyproject.toml)

**ECDHE (Section 11.2)**: IMPLEMENTED in the reference code. The
`seal_bus_message_ecdhe()` function generates per-message ephemeral X25519
keypairs with HKDF domain separation (`b"HERMES-ARC8446-ECDHE-v1"`),
extended signature scope (`sign(ciphertext + eph_pub)`), ECDHE AAD binding,
and ephemeral key zeroization. The existing `open_bus_message()` auto-detects
ECDHE mode by the presence of the `eph_pub` field and uses the appropriate
decryption path. Backward compatible with static-mode messages. Test coverage:
8 dedicated ECDHE tests in `test_crypto.py`.

## 11. Future Work

### 11.1 Post-Quantum Cryptography

When NIST PQC standards (FIPS 203 Kyber, FIPS 204 Dilithium) achieve wider
library support, ARC-8446 SHOULD offer a hybrid mode:

- Key agreement: X25519 + ML-KEM (Kyber) combined
- Signatures: Ed25519 + ML-DSA (Dilithium) combined

This provides quantum resistance while maintaining backward compatibility
with classical-only implementations.

**Minimum library version**: Implementations targeting PQC hybrid mode MUST
use `cryptography>=42.0`, which provides ML-KEM support via OpenSSL 3.2+.

**Migration trigger**: Migration to hybrid mode SHALL be initiated when:
(a) the `cryptography` library ships stable ML-KEM/ML-DSA support, AND
(b) all participating clans agree to upgrade via bilateral explicit agreement.
Unilateral migration MUST NOT be performed, as it would break interoperability.

**Harvest-now-decrypt-later threat**: Adversaries with access to relay traffic
MAY store encrypted messages today for decryption once quantum computers become
viable. Clans exchanging high-sensitivity messages SHOULD evaluate the timeline
for PQC migration against the sensitivity lifetime of their data and consider
accelerating adoption when stable library support becomes available.

### 11.2 Ephemeral Key Exchange (ECDHE Forward Secrecy)

This section specifies per-message ephemeral Diffie-Hellman key exchange
(ECDHE) for HERMES, providing forward secrecy: compromise of a clan's static
X25519 private key does NOT allow decryption of past messages sealed under
ephemeral keys.

#### 11.2.1 Overview

Each sealed message under ECDHE mode uses a fresh, single-use X25519 keypair
generated by the sender. The ephemeral private key is used exactly once — to
derive a shared secret with the receiver's static X25519 public key — and MUST
be zeroized from memory immediately after derivation. The ephemeral public key
is included in the sealed envelope so the receiver can derive the same shared
secret.

This design provides **per-message forward secrecy**: even if the receiver's
static X25519 key is later compromised, an attacker cannot recover past shared
secrets because each depended on an ephemeral private key that no longer exists.

#### 11.2.2 Sender Procedure (ECDHE Seal)

To seal a message with forward secrecy, the sender MUST perform these steps
in order:

1. **Generate ephemeral keypair**: Create a fresh X25519 keypair
   `(eph_priv, eph_pub)` using a cryptographically secure random number
   generator. This keypair MUST NOT be reused across messages.

2. **Derive raw shared secret**: Compute the X25519 key agreement between the
   ephemeral private key and the receiver's static X25519 public key:
   ```
   raw_shared = X25519(eph_priv, peer_static_dh_pub)
   ```

3. **Zeroize ephemeral private key**: The sender MUST zeroize `eph_priv` from
   memory immediately after computing `raw_shared`. The ephemeral private key
   MUST NOT be stored to disk, logged, or retained in any form. This step is
   the foundation of forward secrecy — delaying or omitting it negates the
   security guarantee entirely.

4. **Derive shared secret via HKDF**: Apply HKDF-SHA256 to produce the
   encryption key:
   ```
   shared_secret = HKDF-SHA256(
       ikm    = raw_shared,
       salt   = None,
       info   = b"HERMES-ARC8446-ECDHE-v1",
       length = 32
   )
   ```
   The `info` string `b"HERMES-ARC8446-ECDHE-v1"` provides domain separation
   from the static-mode `info` string `b"HERMES-ARC8446-v1"` (Section 5.1),
   ensuring that ECDHE-derived keys and static-derived keys occupy distinct
   cryptographic domains even if the same raw DH output were somehow produced.

5. **Generate nonce**: 12 random bytes (96-bit IV for AES-256-GCM), per
   Section 6.1.

6. **Construct AAD**: Build ECDHE-mode AAD per Section 11.2.5.

7. **Encrypt**: `ciphertext = AES-256-GCM.encrypt(shared_secret, nonce, plaintext_utf8, aad=aad_bytes)`

8. **Sign**: Compute the Ed25519 signature over the concatenation of
   ciphertext bytes and ephemeral public key bytes:
   ```
   signature = Ed25519.sign(sender_sign_priv, ciphertext_bytes + eph_pub_bytes)
   ```
   This binds the sender's static identity to both the encrypted content and
   the specific ephemeral session, preventing an attacker from substituting
   `eph_pub` without invalidating the signature.

9. **Assemble envelope**: Construct the sealed message per Section 11.2.4.

#### 11.2.3 Receiver Procedure (ECDHE Open)

To open an ECDHE-sealed message, the receiver MUST perform these steps:

1. **Detect ECDHE mode**: If the sealed envelope contains an `eph_pub` field,
   the message was sealed under ECDHE mode. If `eph_pub` is absent, fall back
   to static DH opening per Section 7.1.

2. **Verify signature**: Verify the Ed25519 signature against the concatenation
   of ciphertext bytes and ephemeral public key bytes:
   ```
   Ed25519.verify(peer_sign_pub, ciphertext_bytes + eph_pub_bytes, signature)
   ```
   The `peer_sign_pub` MUST be loaded from `keys/<clan_id>.pub`, NOT from
   the envelope's `sender_sign_pub` field (see Section 6.2 security note).
   If verification fails → REJECT the message.

3. **Derive raw shared secret**: Compute the X25519 key agreement between the
   receiver's static X25519 private key and the sender's ephemeral public key:
   ```
   raw_shared = X25519(my_static_dh_priv, eph_pub)
   ```

4. **Derive shared secret via HKDF**: Apply HKDF-SHA256 with the same
   parameters as the sender:
   ```
   shared_secret = HKDF-SHA256(
       ikm    = raw_shared,
       salt   = None,
       info   = b"HERMES-ARC8446-ECDHE-v1",
       length = 32
   )
   ```

5. **Reconstruct AAD**: Build the ECDHE-mode AAD per Section 11.2.5.

6. **Decrypt**: `plaintext = AES-256-GCM.decrypt(shared_secret, nonce, ciphertext, aad=aad_bytes)`

7. **Decode**: UTF-8 decode the plaintext bytes.

#### 11.2.4 ECDHE Sealed Envelope Format

An ECDHE-sealed message MUST use the following envelope format:

```json
{
  "ciphertext": "<hex-encoded ciphertext + GCM tag>",
  "nonce": "<hex-encoded 12-byte nonce>",
  "signature": "<hex-encoded 64-byte Ed25519 signature over ciphertext+eph_pub>",
  "sender_sign_pub": "<hex-encoded 32-byte Ed25519 public key>",
  "eph_pub": "<hex-encoded 32-byte X25519 ephemeral public key>",
  "aad": "<hex-encoded canonical JSON AAD>",
  "enc": "ed25519+x25519e+aes256gcm+hkdf",
  "src_clan": "<clan_id>"
}
```

Field semantics:

- **`eph_pub`**: The sender's ephemeral X25519 public key, hex-encoded. Its
  presence signals ECDHE mode to the receiver.
- **`enc`**: The encryption algorithm descriptor. The value
  `"ed25519+x25519e+aes256gcm+hkdf"` indicates ECDHE mode. The `x25519e`
  component (note the trailing `e` for "ephemeral") distinguishes this from
  static-mode `x25519`. Implementations MAY use the `enc` field to select
  the decryption path.
- **`src_clan`**: Sender clan identifier, for routing and key lookup.
- All other fields retain the same semantics as the base format (Section 6.2).

#### 11.2.5 ECDHE AAD Construction

In ECDHE mode, the AAD MUST include the ephemeral public key to
cryptographically bind the ephemeral session to the envelope metadata. The
ECDHE AAD extends the base AAD (Section 6.1.1) with the `eph_pub` field:

```
aad_bytes = canonical_json({
    "dst": dst,
    "eph_pub": eph_pub_hex,
    "src": src,
    "ts": ts,
    "type": type
})
```

The `eph_pub` field MUST be included in the canonical JSON object with its
hex-encoded value. Because canonical JSON sorts keys alphabetically, `eph_pub`
falls between `dst` and `src`, ensuring deterministic byte ordering.

**Security rationale**: Including `eph_pub` in the AAD prevents a
Man-in-the-Middle from substituting the ephemeral public key in the envelope
while leaving the ciphertext intact. Without this binding, an attacker could
replace `eph_pub` with their own key, derive the shared secret using their
own private key, re-encrypt the plaintext, and present it as the original
message. By binding `eph_pub` to the AAD, any substitution causes AES-256-GCM
decryption to fail with an authentication error.

#### 11.2.6 Signature Scope in ECDHE Mode

In ECDHE mode, the signature scope differs from static mode (Section 6.3).
The sender MUST sign the concatenation of ciphertext bytes and ephemeral
public key bytes (both as raw bytes, not hex strings):

```
signed_data = ciphertext_bytes + eph_pub_bytes
signature = Ed25519.sign(sender_sign_priv, signed_data)
```

**Rationale**: In static mode, signing only the ciphertext suffices because
the encryption key is deterministically derived from static keys — there is
no ambiguity about which key was used. In ECDHE mode, the encryption key
depends on the ephemeral keypair. Signing the ciphertext alone would allow an
attacker to substitute `eph_pub` in the envelope without detection at the
signature verification stage (detection would only occur at AAD verification
during decryption). By including `eph_pub` in the signed data, the sender's
static Ed25519 identity is bound to the specific ephemeral session, providing
defense-in-depth alongside the AAD binding (Section 11.2.5).

An implementation MUST NOT sign only the ciphertext in ECDHE mode.

#### 11.2.7 Ephemeral Key Zeroization

The ephemeral private key `eph_priv` MUST be zeroized from memory immediately
after the raw shared secret `raw_shared` is computed in step 2 of the sender
procedure (Section 11.2.2). Specifically:

- Zeroization MUST occur before encryption (step 7), before signing (step 8),
  and before any I/O operation that could cause the key to be paged to disk.
- Zeroization SHOULD use a constant-time memory wipe function (e.g.,
  `SecretBox.destroy()` or direct `memset` via FFI) to prevent compiler
  optimization from eliding the wipe.
- The ephemeral private key MUST NOT be written to disk, logged, serialized,
  or transmitted under any circumstance.
- Implementations SHOULD verify zeroization in their test suite by asserting
  that the key material is overwritten after use.

**Why this matters**: Forward secrecy depends entirely on the ephemeral private
key ceasing to exist. If `eph_priv` is recoverable from memory, swap, or core
dumps, an attacker who later compromises the endpoint can derive the shared
secret and decrypt the message, defeating the purpose of ECDHE entirely.

#### 11.2.8 Canonical Parameters (v1.2)

Implementations MUST use the following canonical parameters for ECDHE mode:

| Parameter | Canonical Value | Rationale |
|-----------|----------------|-----------|
| HKDF info | `HERMES-ARC8446-ECDHE-v1` | Semver-style versioning for domain separation |
| AAD scope | Canonical JSON with `eph_pub` included | Defense-in-depth: prevents MITM ephemeral key substitution via both signature AND AAD |
| Signature scope | `ciphertext_bytes + eph_pub_bytes` | TLS 1.3 Finished message convention |

**HKDF Info String**: The `info` parameter for HKDF-SHA256 MUST be the byte string `b"HERMES-ARC8446-ECDHE-v1"`. This provides domain separation from static-mode derivation and from non-canonical implementations.

**AAD Construction**: The AAD MUST be constructed as canonical JSON (sorted keys, no whitespace separators `(",",":")`) that includes the `eph_pub` field alongside envelope metadata (src, dst, ts, type). Including `eph_pub` in AAD provides defense-in-depth against ephemeral key substitution attacks.

**Signature Scope**: The Ed25519 signature MUST cover the concatenation of raw `ciphertext_bytes` followed by raw `eph_pub_bytes` (not hex strings). This follows the TLS 1.3 convention for Finished message signatures.

#### 11.2.9 Migration from Non-Canonical Implementations (v1.2)

During bilateral interop testing (QUEST-003, March 2026), the following non-canonical parameter choices were observed in independent implementations:

| Parameter | Non-Canonical Variant | Risk |
|-----------|----------------------|------|
| HKDF info | `HERMES-ARC8446-v3-ECDHE` | Key derivation incompatible with canonical |
| AAD scope | Metadata only (no `eph_pub`) | Reduced defense-in-depth |
| Signature scope | `eph_pub_bytes + ciphertext_bytes` (reversed) | Signature verification incompatible |

**Migration Period**: For 30 days from the publication of ARC-8446 v1.2, receivers SHOULD implement the following fallback behavior:

1. Attempt decryption with canonical parameters first
2. If signature verification fails, try reversed signature scope (`eph_pub || ciphertext`)
3. If decryption fails, try AAD without `eph_pub`
4. If decryption fails, try non-canonical HKDF info `HERMES-ARC8446-v3-ECDHE`

Senders MUST always use canonical parameters. The fallback behavior is receiver-side only and does not weaken the security model — it merely accepts messages from implementations that have not yet aligned.

After the migration period, receivers MAY remove fallback behavior. Implementations that have not aligned by then will lose interoperability.

**Non-canonical parameters are DEPRECATED** and will not be supported in future versions.

#### 11.2.10 Backward Compatibility

ECDHE mode is backward compatible with static-mode implementations:

- **Detection**: Receivers MUST detect ECDHE mode by the presence of the
  `eph_pub` field in the sealed envelope. If `eph_pub` is absent, the message
  MUST be processed using the static DH path (Section 7.1).

- **Preference**: Receivers that support ECDHE MUST prefer ECDHE-sealed
  messages over static-sealed messages when both modes are available from the
  same sender. This ensures the strongest available security is used.

- **Strict mode**: Implementations MAY support a `require_ecdhe` configuration
  flag. When `require_ecdhe=true`, the receiver MUST reject messages that do
  not include `eph_pub`. This allows clans to enforce forward secrecy as a
  hard requirement once both parties have upgraded.

- **Mixed-mode period**: During migration, a sender MAY send messages in either
  mode. The receiver MUST handle both correctly. Clans SHOULD coordinate the
  transition and enable `require_ecdhe` bilaterally once both sides confirm
  ECDHE support.

#### 11.2.11 Nonce Registry Applicability

The nonce registry requirements of Section 9.6 apply equally to ECDHE mode.
Nonces MUST be tracked per sender clan regardless of whether the message was
sealed with static or ephemeral keys. The nonce-set does not need to
distinguish between modes — a nonce is simply a unique identifier for a
message from a given sender.

#### 11.2.12 Security Properties

ECDHE mode provides the following additional security properties beyond the
base ARC-8446 protocol:

| Property | Static Mode | ECDHE Mode |
|----------|-------------|------------|
| Confidentiality | Yes | Yes |
| Authenticity | Yes (Ed25519) | Yes (Ed25519) |
| Integrity | Yes (GCM + signature) | Yes (GCM + signature) |
| Metadata binding | Yes (AAD) | Yes (AAD + eph_pub) |
| Forward secrecy | No | **Yes** |
| Identity-ephemeral binding | N/A | **Yes** (signature over ciphertext+eph_pub) |
| MITM resistance on eph_pub | N/A | **Yes** (AAD binding + signature) |

**Threat mitigated**: An attacker who compromises a clan's static X25519
private key can decrypt future messages (until key revocation per Section 9.7)
but CANNOT decrypt past ECDHE-sealed messages, because each past message's
shared secret depended on an ephemeral private key that was zeroized
immediately after use.

### 11.3 Extended Associated Data

Basic AAD binding (sender, destination, message type) is specified in
Section 6.1.1. Future work MAY extend the AAD scope to include additional
fields such as timestamps, sequence numbers, or protocol version identifiers.
Extended AAD would provide stronger cryptographic binding between the envelope
metadata and the encrypted payload, further limiting the attack surface for
metadata manipulation.

## 12. References

- RFC 8446 — The Transport Layer Security (TLS) Protocol Version 1.3
- RFC 7748 — Elliptic Curves for Security (X25519)
- RFC 8032 — Edwards-Curve Digital Signature Algorithm (Ed25519)
- NIST SP 800-38D — Recommendation for GCM Mode
- NIST FIPS 203 — Module-Lattice-Based Key-Encapsulation Mechanism (ML-KEM)
- NIST FIPS 204 — Module-Lattice-Based Digital Signature (ML-DSA)
- [ARC-5322](ARC-5322.md) — HERMES Message Format
- [ARC-3022](ARC-3022.md) — Agent Gateway Protocol
- [ARC-2119](ARC-2119.md) — Requirement Level Keywords
